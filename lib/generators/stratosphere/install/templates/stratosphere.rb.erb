Stratosphere.configure do |config|
  config.cloud      = :aws
  config.domain     = 'http://s3.amazonaws.com/<%= @bucket %>'
  config.aws        = {
      access_key: '<%= @key %>',
      secret: '<%= @secret %>',
      region: 'us-east-1',
      s3_bucket: '<%= @bucket %>'
  }
end